/** Attiva la modalità dimostrazione (switch Tutor/Studente, UI semplificata). */
export const IS_DEMO_MODE = true

/** Mostra shop, estratto conto e stile gamification completo. */
export const SHOW_GAMIFICATION = false
